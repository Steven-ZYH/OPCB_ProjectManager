# R01.3 前置契约层执行记录

执行日期：2026-08-06 CST  
隔离候选：`/private/tmp/opcb-r01-candidate.y4OZmP/repo`  
候选分支：`codex/r01-contract-foundation`  
基线 commit：`5eaa4267ffe9ac63ba63982b9c07c4f57532f793`  
契约 commit：`193b289c51b611f7c4321af22ff0625c05345beb`  
路线图 shadow commit：`694a27aeb224ffea8b8278c61bffad762fd126e9`
PR 概览 shadow commit：`1ee9b30febaa819c699aae1f9d1738d640e1482d`
Review Machine 状态 shadow commit：`e3d6f883b1691d2470b4840a2ca6b1f97a65b322`
Runtime Facade 收口 commit：`ecef3c3032c57a197f49d455d5dfebebc84f464c`

## 结论

- R03.1 Project Registry v1 基础合同：**通过**。
- R09.1 Audit Event envelope 基础合同：**通过**。
- R12.1 `Observed<T>` / Freshness 状态机基础合同：**通过**。
- R01.3 Legacy Facade：**缓存三切片及 Session、GitHub live、审核机 Runtime Facade 已接线且通过；默认仍走纯旧路径，未切换 UI 读结果**。
- 原源码脏工作树、正式安装版、视觉与运行进程均未修改；无 push、PR、部署或外部写操作。

## 新增模块

新增独立 Swift Package `ProjectControlDomain`，与现有单体 `swiftc` App 构建保持解耦：

| 合同 | 已实现边界 |
|---|---|
| Project Registry v1 | 稳定 `projectID`、仓库身份、checkout、默认分支、路线图、Session 范围、环境、Provider、Policy、Capability、字段来源；显式 schema version/migration；未知版本与嵌套未知字段 fail-closed；绝对路径不能作为项目身份 |
| Audit Event | project/resource/category/source/time/generation/actor/command/evidence envelope；observation、claim、intent、confirmation、execution、verification、correction 分型；去重、乱序/迟到/冲突判定；执行与确认强制 commandID；敏感字段 fail-closed |
| Observed/Freshness | `live/cached/fetching/stale/unreachable/partial/unknown`；value/source/observedAt/expiresAt/generation/error；TTL 过期；乱序丢弃与恢复；空结果、缺证据、不支持、Provider 失败、超时、离线分离 |

可携带 fixture `opcb-registry-v1.json` 明确区分稳定项目身份与本机绝对 checkout，并为 repository/defaultBranch 记录字段来源。

## 验证结果

### Swift Package

命令：`swift test`（模块缓存定向到 `/private/tmp`）

- 22 tests，0 failures。
- Registry：round-trip、OPCB fixture、迁移、未知版本/字段、重复 ID、绝对路径身份拒绝。
- Audit：round-trip、claim/执行/验证分离、commandID、去重/乱序/冲突、隐私字段拒绝。
- Observed：TTL、空成功、unknown/unreachable、超时/不支持/缺证据、离线与恢复、旧 generation 丢弃。

### 原有 Native Harness

命令：`./test-native.sh`

- 16 个 Native/Worker smoke 全部 PASS（包括缓存与 Runtime Facade smoke）。
- AppIconBundle PASS，候选 App 成功生成并签名。
- 最终结果：`native test suite: PASS`。

## 可恢复证据

| 文件 | 用途 |
|---|---|
| `contract-foundation.patch` | 可审阅的 full-index binary patch，不含 `.build` |
| `private/contract-foundation.bundle` | 完整本地 Git 历史与 `codex/r01-contract-foundation` ref；`git bundle verify` 通过 |

哈希统一记录在 `SHA256SUMS`。

## 明确删除门

路线图、PR 概览与 Review Machine 状态缓存按“旧 Store 输出 → Legacy Facade 标准化 → 新 Adapter 独立读取同一文件 → 字段级差异审计”完成；Session、GitHub live 与审核机 Runtime 结果也已全部经过 Facade。审核机写命令只执行一次，Shadow 只比较意图和回执。

候选结果尚未取代 UI 权威结果，因此仍不能删除旧 timer、cache 或 client。后续切换必须逐项进入 candidate-primary，并保留独立回退。任何 `unknown/unreachable` 都必须原样保留，不得补成空、健康或完成。
