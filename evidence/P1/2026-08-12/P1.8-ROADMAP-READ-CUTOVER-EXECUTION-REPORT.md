# P1.8 + R01.4 Roadmap 首个只读切流执行报告

生成时间：2026-08-12 CST

执行仓库：`Steven-ZYH/sixlab-pr-control`

PR：[P1.8: add fail-closed Roadmap read cutover #21](https://github.com/Steven-ZYH/sixlab-pr-control/pull/21)

```text
VERDICT: READY_FOR_INDEPENDENT_REVIEW
BASE_SHA: 7af0430230bebd4c4a1477b48cd9b624072e0b76
HEAD_SHA: 2df7f39ff5bed312d7c004ce9d61a582d7d5a4b1
MERGE_TREE: 1749a951641669be50ebf8f6b7b9959f60ef0818
SCOPE:
- Roadmap-only candidate-primary mode — PASS
- exact Runtime -> SQLite projection route — PASS
- strict selection and typed legacy fallback — PASS
- all other Facades remain legacy/shadow — PASS
CHECKS:
- swift test — PASS (102 tests, 0 failures)
- test-native.sh — PASS (exact-head revision guard and source binding)
- diff/merge-tree checks — PASS
CURRENT PR STATE:
- open/Ready/mergeable — YES
- requested independent reviewer — miaopantao
- exact-head approvals — NONE
- unresolved review threads — 0
- CI — NOT YET REPORTED
BOUNDARIES:
- protected dirty primary worktree — untouched
- App installation or candidate launch — not performed
- legacy Roadmap retirement — not performed
- other Facade cutover — none
- command/worker/deploy — none
NEXT AUTHORITY NEEDED: current-head independent approval and green CI before merge
```

## 范围选择证据

- 执行前重新 fetch 发现 P1.7 merge 后 main 已被 PR #18 推进到
  `7af0430230bebd4c4a1477b48cd9b624072e0b76`；本实现没有复用旧 base，而是在新 main 的
  隔离 worktree 重新完成 AppDelegate 组合与完整验证。
- Roadmap 是 R01.3 最早完成的只读 shadow：冻结 9 documents、42 items，业务字段、timeline、
  freshness 和 error 为 0 个未解释差异；只有 source system 是已说明差异。
- Roadmap 不携带 approval、Session、review-machine、secret 或 command 权限，且单一缓存与
  30 分钟 TTL 可以保持 UI 语义不变，因此被选为最小、可逆的首次切流。

## 实现证据

- `LegacyFacadeMode` 仅在 Roadmap 增加 `candidatePrimary`；只接受环境值
  `candidate-primary`。默认、缺失和其他字符串仍为 `legacyOnly`。
- 新增 exact resource：`opcb / roadmap-cache / roadmaps.json`。`RoadmapProjectionCutover`
  通过 P1.7 Runtime route 执行 durable begin/observation，并写入独立私有
  `SQLiteNormalizationStore<RoadmapProjection>`。
- Roadmap Facade 只获得同步 read-only closure；不持有 SQLite commit、runtime schedule、
  adapter refresh 或 provider command 权限。
- candidate-primary 依次验证 exact resource、无 active refresh、projection 与 last-valid 都是
  `live`／`cached`、Roadmap stage 可严格转换，以及字段比较 0 个未解释差异。
- 允许差异只有两项：legacy/candidate source system 身份不同；runtime monotonic generation
  与 legacy cache timestamp generation 的机制不同。business fields、locator、timeline、
  freshness 与 error 均不在允许表。
- missing、read failure、stale、in-flight、resource mismatch、invalid stage、comparison failure
  或 field drift 返回 legacy，并记录 typed fallback；不会生成空路线图或健康状态。
- live fetch 成功后仍先保存兼容 `roadmaps.json`，再由 Runtime normalize，随后 UI 重新通过
  Facade 选择值。legacyOnly/shadow 的旧行为和 30 分钟 schedule 保持。
- Snapshot、Review Machine status 与 Runtime Facade 的 mode 源文件都由 architecture test
  证明没有 `candidatePrimary`／`candidate-primary`。

## 验证证据

- fail-first architecture test 先因 Roadmap 缺少 candidate mode 产生 2 个失败，随后实现通过。
- `swift test --disable-sandbox`：102 tests，0 failures；architecture tests 为 5 tests。
- `NativeRoadmapFacadeSmoke` 覆盖 runtime durable refresh、SQLite reopen 后仍可读、candidate
  success、missing、reader throw、stale、active refresh、wrong exact resource、invalid stage、
  field drift fallback，以及 unknown mode 回到 legacyOnly。
- `CI_EXPECTED_SHA=2df7f39ff5bed312d7c004ce9d61a582d7d5a4b1 ./test-native.sh`：PASS。
  RevisionGuard、SwiftPackageTests、全部 native smoke/render、ReviewWorker、AppIcon、codesign
  与 SourceBinding 通过；SourceBinding 绑定 exact clean head。
- 刷新远端后 `origin/main` 仍为
  `7af0430230bebd4c4a1477b48cd9b624072e0b76`，也是本分支 merge-base。
- `git diff --check origin/main...HEAD`：PASS。
- `git merge-tree --write-tree origin/main HEAD`：PASS，merge tree
  `1749a951641669be50ebf8f6b7b9959f60ef0818`。
- PR #21 当前为 open、Ready、mergeable，head/base 与本报告一致；已请求 `miaopantao`，尚无
  approval 或 review thread。GitHub 当前尚未返回 CI status，不能视为 green。

## 权限与下一门

本轮只完成代码、测试、文档、分支、推送和 Ready PR。没有合并、安装、候选 App 启动、
正式偏好修改、legacy reader 退休、Worker 更新或环境部署。即使 PR 后续合并，R01.4 的候选
构建、隔离运行、正式安装、回滚载体和观察期仍需分别授权、分别举证。
