# P1.7 Runtime Actor + RefreshCoordinator 模块关闭记录

关闭时间：2026-08-12 CST

执行仓库：`Steven-ZYH/sixlab-pr-control`

PR：[P1.7: add runtime actor and replay-safe command journal #19](https://github.com/Steven-ZYH/sixlab-pr-control/pull/19)

## 关闭结论

P1.7 已关闭。PR head `84901003c3daae446cd79f718b4694a0d37e257a` 在 CircleCI 成功、
0 个 unresolved review thread、`miaopantao` 独立 exact-head 批准后，以 expected-head gate
squash merge 为 `acca75a10ae0a707f6398f957feca42a585428f6`。

合并前 merge-tree `823e52ba9855f76f1534f1e9a3258cea8b4c8cf4` 与实际 merge commit
tree 完全一致。PR-head run 48 和 main run 52 均成功；actual merge commit 的完整 native
harness 通过，RevisionGuard 与 SourceBinding 均绑定 exact clean merge SHA。

## 已关闭能力

- 单一 `ProjectControlRuntime` actor 持有 refresh lease、generation、cancellation、business
  schedules、legacy activity claim 与 projection stream。
- AppDelegate 不再持有 GitHub/session/review-machine/roadmap 的业务 Timer 或对应 in-flight
  authority；UI animation timer 仍留在 UI 层。
- SQLite confirmation/command journal 提供 authoritative issuance、exact binding、atomic
  claim、reopen persistence、concurrent consumer protection、private permissions 与 schema/
  integrity fail-closed。
- command 在执行前 durable claim；duplicate 不重复执行。claim 后 crash window 保持
  `claimed` unknown，不自动解释为 safe-to-retry。
- Runtime 只接 closed/fake executor；没有真实 merge、deploy 或 secret side effect。

## 未被本次关闭覆盖

- 六路 Facade 的 candidate/read cutover、安装验收、回退观察与 legacy read retirement；
- 真实 provider command executor、Worker 更新、secret retrieval；
- staging 或 production 部署。

以上均不由 PR merge、CircleCI 或 post-merge native harness 自动授权。

## 下一阶段

P1.8 + R01.4：一次只选择一个只读 projection，先 shadow/candidate，完成独立验证、安装验收
与回退观察后，才允许退休对应 legacy read path。开始 P1.8 需要新的明确执行请求。
