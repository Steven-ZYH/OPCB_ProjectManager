# P1.2／P1.3-A 控制平面内核模块关闭记录

关闭时间：2026-08-11 CST

模块状态：**CLOSED**

范围：Swift Package 最小强制依赖边界，以及 Project Registry 的 schema、文件持久化、
迁移、隐私校验、原子替换、last-known-good 恢复与 Legacy Facade 兼容接入。

## 关闭结论

本模块已完成代码、负向回归、独立 current-head 审核、CI、合并、从 merged main
重建、正式 App 安装、运行态 Registry 初始化、回滚载体与部署面核验。

| 门 | 当前事实 |
|---|---|
| 代码 | PR #13 head `c501a6787af1966354a090473f274ec2d424ea69` |
| 合并 | squash commit / live main `846344a6481d93070e47338c06abf207e7b43d2d` |
| 审核 | `APPROVED` 正文显式绑定 current head；合并前 0 unresolved thread |
| CI | CircleCI `native-build-and-test` run 27 SUCCESS |
| 构建 | clean detached main；38/38 Swift tests；18 个 Harness 入口 PASS |
| 安装 | 正式 App 二进制 SHA-256 `820d65448bad53cfe3760784ff19b8772b890e56fb3651334f3886f72f38922f` |
| 来源绑定 | `OPCBSourceSHA=846344a6481d93070e47338c06abf207e7b43d2d`，tree `clean` |
| 运行 | PID `30180` 从正式 Applications 路径运行 |
| Registry | schema v1、mode `0600`、4 个项目及顺序已跨进程落盘 |
| Worker | 本 PR 无 Worker diff；保留较新的 live `1.10.5-fast.1`，不降级 |
| 回滚 | 安装前 App、Application Support、Preferences 私有备份和 manifest 已验证 |

## 已关闭能力

- `ProjectControlDomain <- ProjectControlStore` 与独立只读
  `ProjectControlAdapters` 依赖方向可测试。
- Registry v0 -> v1 迁移、未知版本/未知字段/重复身份 fail-closed。
- 内置 catalog 与 `SIXLAB_PR_PROJECTS` 幂等导入。
- mode `0600` 临时文件、fsync、同目录 rename 原子替换。
- 损坏 JSON 可从经隐私校验的 last-known-good 恢复，后续合法写入可替换损坏主文件。
- primary、last-known-good 与写入路径共用 sensitive-data gate。
- Swift package XCTest 已进入 CircleCI 权威 `test-native.sh` 入口。

## 不得误报为完成

- 整个 P1 阶段尚未关闭。
- P1.3-B 的 repository access/default branch/provider/environment 健康检查，以及
  App 内 Registry CRUD/排序/禁用，迁移到 P2.2 原生仓库管理闭环。
- State Normalizer 的 keyed projection、expected-generation CAS、部分失败合成与
  refresh ownership 尚未完成。
- SQLite 事件账本、Policy Engine、Runtime Actor、`candidatePrimary` 与任何读路径
  切流尚未完成。
- 没有新增 Worker、服务端、staging、canary 或 production 发布产物。

## 后续入口

- [正式安装报告](./install/INSTALL-REPORT.md)
- [后续顺序与缺口重评估](./P1-REASSESSMENT.md)
- [P1.4 State Normalizer START Handoff](./P1.4-STATE-NORMALIZER-START-HANDOFF.md)
- [PR #13](https://github.com/Steven-ZYH/sixlab-pr-control/pull/13)
