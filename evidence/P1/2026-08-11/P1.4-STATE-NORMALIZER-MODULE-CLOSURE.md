# P1.4 State Normalizer 模块关闭记录

关闭时间：2026-08-11 CST

模块状态：**CLOSED**

范围：按资源隔离的 normalized projection、refresh/observation envelope、monotonic
generation、expected-generation CAS、begin/accept/fail/expire reducer，以及晚到、重复、
冲突、失败保值和过期语义。

## 关闭结论

P1.4-A 已完成代码、负向回归、current-head 独立审核、PR head CI、明确 merge 授权、
合并、合并后 main CI 和 clean merged-main 完整 Native Harness。该模块是未接真实
provider/runtime 的纯内核切片，因此关闭不以 App 安装为门；未安装、未更新 Worker、
未切流和未部署仍被明确保留为未执行事实。

| 门 | 当前事实 |
|---|---|
| 代码 | PR #14 head `64fec37720c8d0d7b93d9a2895388a26d95f064b` |
| 合并 | squash commit / live main `7d50c14e9e926a3bc3a3a31c9554ff6f63403642` |
| 审核 | 两条 `APPROVED` 绑定 current head；旧 head 的 requested changes 已修复；合并前 0 unresolved thread |
| PR CI | CircleCI `native-build-and-test` run #36 SUCCESS |
| main CI | merge commit 的 CircleCI `native-build-and-test` run #38 SUCCESS |
| 构建 | clean detached merged main；完整 `test-native.sh` PASS |
| 来源绑定 | `OPCBSourceSHA=7d50c14e9e926a3bc3a3a31c9554ff6f63403642`，tree `clean` |
| 签名／图标 | codesign verify、AppIcon bundle 均 PASS |
| 权限 | Steven 明确授权 merge；未授权也未执行安装、Worker 更新或部署 |

## 已关闭能力

- `NormalizedResourceKey` 的 canonical key、结构 equality/hash 和 Store lookup 统一使用
  精确、大小写敏感字段。
- refresh 与 observation 精确绑定 resource、generation、correlation、source、startedAt，
  防止伪造来源或被替换 timeline 的响应进入 projection。
- `begin` 保留上一有效值；`accept`／`fail` 只接受当前 active refresh；`expire` 只使用
  注入的 evaluation time。
- late、duplicate、conflict、invalid、CAS failure 均输出不含 value/provider message 的
  typed decision。
- 资源隔离、partial/unknown/unreachable 语义、失败保值、过期转换和大小写身份反例已
  进入 Swift Package XCTest 与权威 native Harness。
- P1.5 event/projection persistence boundary 已随 main 留档。

## requested changes 收口

旧 head `1bc15007a86021703e9f9822b4f6d2ac54c2be07` 的两个 P1 问题已在
`64fec37720c8d0d7b93d9a2895388a26d95f064b` 修复：

1. observation 现在必须精确匹配 active begin 的 source 与 startedAt；
2. 资源身份的 canonical/equality/hash/store 契约统一为 exact-spelling、大小写敏感。

相应 forged-source、replaced-timeline 和 case-variant 回归均已通过。

## 收口清理

- 已删除远端已合并分支 `codex/p14-state-normalizer`，随后 fetch/prune 确认无同名远端
  或本地分支。
- 已移除 P1.4 实现、live-base 组合验证和 merged-main 收口验证的三个 clean 临时
  worktree。
- 其他 review/worker/PR12 工作树均不属于本模块，未修改、未清理。
- 受保护脏工作树 `/Users/steven/Documents/Project/SIXLABPRControl` 未 reset、rebase、
  clean 或覆盖；其原有修改与未跟踪文件保持不变。

## 不得误报为完成

- 整个 P1 阶段尚未关闭。
- `InMemoryProjectionStore` 不是持久化事实源，不能跨进程恢复或重建。
- SQLite schema、event ledger、transaction、projection rebuild、migration、backup/recovery
  尚未完成。
- Policy Engine、Runtime Actor、RefreshCoordinator、provider 接线、projection stream 和
  `candidatePrimary` 尚未完成。
- 正式安装 App 仍不包含 P1.4 merge commit；没有任何 Worker、staging、canary 或
  production 发布。

## 后续入口

- [P1.4 执行报告](./P1.4-STATE-NORMALIZER-EXECUTION-REPORT.md)
- [后续顺序与缺口重评估](./P1-REASSESSMENT.md)
- [P1.5 SQLite Event Ledger START Handoff](./P1.5-SQLITE-EVENT-LEDGER-START-HANDOFF.md)
- [PR #14](https://github.com/Steven-ZYH/sixlab-pr-control/pull/14)
